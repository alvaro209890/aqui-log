# Evidência — `B2C-05` (foto e campos obrigatórios na criação)

> **Data:** 2026-08-08
> **Agente:** Claude Code (Opus 5)
> **Commit inicial:** `f987e26` (`main`)
> **Decisões:** `DEC-01` (foto obrigatória), `DEC-18` (endereços, peso, tipo, tamanho)
> **Ambiente:** PC `acer`, banco descartável `aqui_log_b2c05`, API em `PORT=3011`
> com `PUBLIC_API_URL=http://localhost:3011/api/v1`

## 1. O que mudou

| Camada | Mudança |
| --- | --- |
| `CreateDeliveryDto` | `productType`, `packageSize`, `weightKg` e `productPhotoUrls` (≥ 1) passam de opcionais a **obrigatórios**; mensagens de erro em português |
| `CreateDeliveryDto` | endereços e nome do destinatário ganham `@IsNotEmpty` + aparo de espaços; `'   '` deixa de passar |
| `DeliveriesService.create` | some o `?? null` dos campos agora garantidos pelo DTO |
| App cliente (`new_order_screen.dart`) | foto vira obrigatória: botão em vermelho, mensagem inline e envio bloqueado antes de chamar a API |
| `scripts/smoke-test.sh` | cliente sobe foto por `presign purpose=product`; pedido completo; **assert negativo** de que o payload legado é recusado com `400` |
| `docs/03-referencia/02-API.md` | contrato de criação atualizado com a coluna "Obrigatório" |

O fallback de `notes` **não** foi tocado.

## 2. Rejeição na criação (HTTP vivo)

Todos contra `POST /deliveries` com token de `CUSTOMER`, na API em `:3011`.

| Caso | Resultado |
| --- | --- |
| payload completo | `201` |
| sem `productType` | `400` — "Informe o tipo da encomenda" |
| sem `packageSize` | `400` — "Informe o tamanho da encomenda (SMALL, MEDIUM ou LARGE)" |
| sem `weightKg` | `400` — "Informe o peso da encomenda em kg" |
| sem `productPhotoUrls` | `400` — "Envie ao menos uma foto da encomenda" |
| `productPhotoUrls: []` | `400` — "Envie ao menos uma foto da encomenda" |
| sem `pickupAddress` | `400` — "Informe o endereço de coleta" |
| sem `deliveryAddress` | `400` — "Informe o endereço de entrega" |
| `pickupAddress: "   "` | `400` — "Informe o endereço de coleta" |
| `weightKg: 0` | `400` — "O peso deve ser maior que zero" |
| foto fora do storage | `400` — "productPhotoUrl deve apontar para o storage da plataforma" |

Depois do ajuste de mensagens, o pedido sem nenhum campo de encomenda devolve
apenas texto em português — nenhuma mensagem crua do `class-validator` sobrou.

## 3. Leitura de pedido legado (critério central)

Linha inserida direto no banco, sem `product_type`, `package_size`, `weight_kg`
nem fotos, com a encomenda no formato histórico de `notes`
(`AQL-LEGADO-B2C05`, id `32624b18-…`):

| Verificação | Resultado |
| --- | --- |
| `GET /deliveries` (cliente dono) | `200` — legado aparece junto do novo |
| `GET /deliveries/:id` (cliente dono) | `200` — `productType: null`, `productPhotoUrls: []`, `notes` histórico intacto |
| `GET /deliveries/:id/history` | `200` |
| `GET /deliveries` (admin) | `200` — os 3 pedidos, legado incluído |
| `GET /deliveries?productType=ELECTRONICS` | legado **fora** do filtro, como especificado em `B2C-01B` |

O parser `OrderMeta.fromDeliveryJson` do core já cobre o fallback de `notes`
(teste "DeliverySummary mantem fallback para notes legado", verde).

## 4. Comandos executados

| Comando | Resultado |
| --- | --- |
| `pnpm build` | PASS (backend + dashboard) |
| `pnpm lint` | PASS |
| `pnpm test` | PASS — backend 10 suítes / **44 testes** (eram 36) |
| `pnpm smoke` (API `:3011`) | PASS — 5 execuções, códigos distintos |
| smoke com expectativa invertida (`201` no lugar de `400`) | FALHA esperada — `exit=1`, prova que o assert negativo é vivo |
| `flutter analyze` (customer_app) | PASS — "No issues found" |
| `flutter test` (customer_app) | PASS — 11 testes (era 10) |
| `flutter analyze` / `flutter test` (courier_app) | PASS — 7 testes |
| `dart analyze` / `dart test` (aqui_log_core) | PASS — 6 testes |
| `DATABASE_NAME=aqui_log_b2c05 pnpm db:migrate` | PASS — 8 migrations |
| APK / QA em emulador ou dispositivo | **NÃO EXECUTADO** |
| QA de navegador do dashboard | **NÃO EXECUTADO** — nenhum arquivo do dashboard mudou |

## 5. Testes novos

Backend (`delivery-package.dto.spec.ts`, +8):

- payload legado (só endereços) é rejeitado nos quatro campos;
- cada campo obrigatório ausente devolve a mensagem própria (4 casos);
- lista de fotos vazia rejeitada;
- `pickupAddress`/`deliveryAddress` ausentes **e** em branco rejeitados;
- coordenada ausente rejeitada.

App cliente (`widget_test.dart`, +1): "NewOrderScreen barra o envio sem foto" —
preenche o formulário inteiro, toca em Publicar e verifica que `onSubmit`
**não** foi chamado e que a mensagem inline aparece.

## 6. Limitações

- Sem APK e sem QA em emulador/dispositivo: a mudança do app cliente está
  provada por teste de widget, não por uso real na tela.
- O banco `aqui_log_b2c05` é descartável; nenhum dado tem valor.
- `fulfillment_mode` (`IMMEDIATE`/`SCHEDULED`) do `DEC-18` **não** entrou: é
  escopo de `B2C-06`/`SCHED-01` e continua fora deste pacote.
